import { createRoot } from 'react-dom/client';

// ADM 壳（INF-1 占位）——ADM-1 起在此实现后台
createRoot(document.getElementById('root')!).render(
  <h1 style={{ fontFamily: 'system-ui', color: '#1B3B2F' }}>雀胡 · ADMIN 壳已就绪</h1>,
);
